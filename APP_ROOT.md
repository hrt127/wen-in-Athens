# Wen in Athens — Application Root

The canonical, actively developed application lives in:

    /streamlit

All other Python files at the repository root are:
- legacy
- experimental
- or historical artifacts

Do NOT import root-level Python modules from the Streamlit app.

If something is needed, it must live inside `/streamlit`.
